export interface Product {
  id: string;
  name: string;
  imageUrl: string;
  price: number;
  category: string;
  categoryKey: string;
  description: string;
  rating: number;
  reviewCount: string;
}
