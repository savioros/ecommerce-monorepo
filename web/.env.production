# web/ — React + Vite
# Copie para web/.env e ajuste os valores.
# Variáveis VITE_ são expostas ao navegador em tempo de build.
# Use web/.env.local para sobrescrever localmente sem commitar.

VITE_API_URL=https://epets-api.onrender.com
VITE_APP_NAME="Petshop"
